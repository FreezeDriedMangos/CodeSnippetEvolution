def add(a, b, c):
    # find the ath register after the current instruction pointer executing this instruction
    # and the bth, then add them into the cth
    # empty a and b into temp values, then add those values into c
    return INSTRUCTION_LENGTH
    
    
def ifjump(a, b, c):
    return 1
    # or return 2
    

def register(a, b, c):
    return noop(a, b, c)
    
    
def noop(a, b, c):
    return 1
    
    
def jump(a, b, c):
    return a
